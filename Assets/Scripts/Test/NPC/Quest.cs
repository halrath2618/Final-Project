using UnityEngine;
using CHARACTERS; // Assuming CHARACTERS namespace contains relevant character classes
using COMMANDS; // Assuming COMMANDS namespace contains relevant command classes  

public class Quest : MonoBehaviour
{

    [SerializeField] private Character character; // Reference to the character involved in the quest
    [SerializeField] private 
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
